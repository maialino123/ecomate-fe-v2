/**
 * Tour Section Component
 *
 * This component now only uses the ImageTourSection for better performance
 * and compatibility across all devices. The 3D version has been removed.
 */

import ImageTourSection from "@/components/image-tour-section";

export default function TourSection() {
  return <ImageTourSection />;
}
